#include <stdio.h>
/**
 * main - is the function used
 * @argc: is the argument to main for numbers
 * @argv: is the first element
 * Return: as specified
 */
int main(int argc, char *argv[])
{

	(void)argv;

	printf("%d\n", argc - 1);

	return (0);
}
