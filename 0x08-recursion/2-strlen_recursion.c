#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen_recursion - a function that shows the string length
 * @s: string
 * Return: as mentioned
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
