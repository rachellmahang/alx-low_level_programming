#include "main.h"
#include <stdio.h>

/**
 *  _strlen_recursion - a function that returns a length of a string
 *  @s: the string
 *  Return: as mentioned
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
